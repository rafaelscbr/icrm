// Arquivo mantido apenas para compatibilidade de imports durante transição.
// Checklist está 100% no banco (coluna JSONB em tasks). Pode ser removido em próxima limpeza.
export function loadChecklists() { return {} }
export function removeChecklist(_taskId: string) {}
